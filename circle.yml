dependencies:
  override:
    - touch _config.env.yml
    - bundle install
    - npm install
    - npm install -g gulp
    - bower install
    - gulp build --production

test:
  post:
    - bundle exec htmlproof ./public
