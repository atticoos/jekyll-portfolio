machine:
  node:
    version: 4.1.0

dependencies:
  override:
    - make circle

test:
  post:
    - bundle exec htmlproof ./public --disable-external

deployment:
  production:
    branch: master
    commands:
      - shipit production deploy
